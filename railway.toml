# Railway 部署配置：https://railway.app
# 在 Dashboard 设置环境变量后 Deploy 即可获得公网 URL

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
