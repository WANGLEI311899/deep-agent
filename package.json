{
  "name": "deep-codex",
  "version": "1.0.0",
  "description": "deepCodex：基于 DeepSeek 的通用型 AI 智能体",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test --test-force-exit tests/*.test.mjs",
    "start": "node dist/server.mjs",
    "dev": "npm run build && node scripts/run-demo.mjs index.mjs",
    "ui": "npm run build && node scripts/run-demo.mjs server.mjs",
    "demo:basic": "npm run build && node scripts/run-demo.mjs",
    "demo:search": "npm run build && node scripts/run-demo.mjs demo-search.mjs",
    "demo:multi": "npm run build && node dist/demo-multi-agent.mjs",
    "rag:status": "npm run build && node dist/rag-cli.mjs status",
    "rag:index": "npm run build && node dist/rag-cli.mjs index",
    "rag:evaluate": "npm run build && node dist/rag-cli.mjs evaluate"
  },
  "dependencies": {
    "@napi-rs/canvas": "^1.0.6",
    "@sentry/node": "^10.69.0",
    "@tesseract.js-data/chi_sim": "^1.0.0",
    "@tesseract.js-data/eng": "^1.0.0",
    "busboy": "^1.6.0",
    "dotenv": "^16.4.0",
    "llamaindex": "^0.12.1",
    "openai": "^6.34.0",
    "pdfjs-dist": "^6.2.108",
    "pino": "^10.3.1",
    "tesseract.js": "^7.0.0"
  },
  "devDependencies": {
    "@types/busboy": "^1.5.4",
    "@types/node": "^22.0.0",
    "pdf-lib": "^1.17.1",
    "tsup": "^8.5.1",
    "typescript": "^5.6.0"
  }
}
